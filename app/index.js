import { registerRootComponent } from 'expo';
import App from '../App'; // Ensure the path is correct and points to App.js

// Register the App component as the root component
registerRootComponent(App);
